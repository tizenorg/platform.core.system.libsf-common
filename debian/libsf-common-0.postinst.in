#!/bin/sh
#if [ ${USER} == "root" ];then
	# change file owner
	# 1.libraries
	#chown root:root /usr/lib/libsf_common.so
#fi
# change file permissions
# 1.libraries
#chmod 644 /usr/lib/libsf_common.so
	
